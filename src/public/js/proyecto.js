const proyecto = document.getElementById('modal_proyecto');

if (proyecto != null && proyecto != "" && proyecto != undefined) {

/*     
    const cargar_responbable = (datos) => {
        fetch("/cargar/responsable", {
            method: "post",
        }).then(res => res.json())
            .then(res => {
                const responsable = document.getElementById('responsable');
                console.log("esta es mi respuesta");
                console.log(res);
                for (let i = 0; i < res.length; i++) {
                    const selector = document.createElement('option');
                    selector.text = res[i].usuario;;
                    responsable.appendChild(selector);
                }
            })
    }

    const main = () => {
        cargar_responbable();
    }

    main(); */


}